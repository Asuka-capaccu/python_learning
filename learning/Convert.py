if __name__ == '__main__':
    # 隐式类型转换 - 自动完成
    # 显式类型转换 - 需要使用类型函数来转换
    print(int("23412", 8))

    # 将x转换为一个整数

    float(100)

    # 将x转换到一个浮点数

    complex(10, 11)

    # 创建一个复数

    print(str(23468927))

    # 将对象x转换为字符串

    # repr(x)

    # 将对象x转换为表达式字符串

    # eval(str)

    # 用来计算在字符串中的有效Python表达式, 并返回一个对象

    tuple(['hello', 'good morning'])

    # 将序列s转换为一个元组

    # list(s)

    # 将序列s转换为一个列表

    # set(s)

    # 转换为可变集合

    # dict(d)

    # 创建一个字典。d
    # 必须是一个(key, value)
    # 元组序列。

    # frozenset(s)

    # 转换为不可变集合

    # chr(x)

    # 将一个整数转换为一个字符

    print(ord(100))

    # 将一个字符转换为它的整数值

    # hex(x)

    # 将一个整数转换为一个十六进制字符串

    # oct(x)
